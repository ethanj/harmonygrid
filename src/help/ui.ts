import keyboardMap from './keyboard-map.svg?raw';
import {studies,studyDocument} from './examples';
import type {InstrumentDocument} from '../document/model';
import './style.css';
export class HelpUI {
 private dialog=document.createElement('dialog');
 constructor(private blocked:()=>boolean,private openExample:(doc:InstrumentDocument)=>void){
  this.dialog.id='help-dialog';this.dialog.setAttribute('aria-labelledby','help-title');document.body.append(this.dialog);
  document.getElementById('help')!.onclick=()=>this.show();
  window.addEventListener('keydown',event=>{if(event.key==='F1'&&!this.blocked()){event.preventDefault();this.show();}});
 }
 show(tab='guide'):void{if(!this.dialog.open&&this.blocked())return;this.render(tab);if(!this.dialog.open)this.dialog.showModal();}
 private render(tab:string):void{
  const guide=`<h2>Your playing keys</h2><figure class="keyboard-map">${keyboardMap}</figure><p class="keyboard-map-caption">Related controls are grouped spatially. L marks keys you can latch with Command. Close Help to play.</p><h2>Play the grid</h2><p>Start sound, then move across the grid. Auto Button plays while you move; switch it off to play only while pressing. Each cell is a lead pitch. The whole selected chord is shown, including notes rejected by the mode.</p><p>With the metronome on, both notes and visualization wait for the next tick. A short press lasts one complete tick by default. Smooth Clavier gives each available scale tone an equal-width region.</p><h2>Keyboard reference</h2><dl><dt>0–9</dt><dd>Select chord slot. Shift XOR Caps Lock selects a mode; both together select chords.</dd><dt>Tab</dt><dd>Set the scale root to the lead pitch.</dd><dt>Space / F</dt><dd>Hold Sustain / toggle Sustain. Notes are retained and released individually.</dd><dt>G / ⌘G</dt><dd>Hold / latch Hold. The most recent chord continues after release.</dd><dt>Y</dt><dd>Toggle Drone. The sounding pitches continue unchanged.</dd><dt>D / ⌘D</dt><dd>Hold / latch Repeat. Reattack on each tick. Drone pitches are protected by default.</dd><dt>T</dt><dd>Toggle metronome quantization.</dd><dt>Q W E R</dt><dd>Hold for ½, 2×, ⅔, ³⁄₂ tempo. Command makes the change permanent. A/S changes one tick per minute.</dd><dt>N / M</dt><dd>Hold to collect chord / mode pitches; release to name and choose a slot. The buttons support click-only collection.</dd><dt>C / V</dt><dd>Copy / paste the selected slot. Shift XOR Caps Lock targets modes. Slot 1 is protected.</dd><dt>⌘/Ctrl S, Shift S, O</dt><dd>Save, Save As, Open. Files include sound, geometry, patterns, and performance settings.</dd><dt>Escape</dt><dd>All Notes Off while playing. Inside an editor, cancel that editor first.</dd></dl><h2>Make your own vocabulary</h2><p>In capture, the first pitch establishes a fixed reference—even if you remove it. Click a pitch again to remove it. Mode capture combines octaves into pitch classes. Auto Button and capture audition eviction start off. Earlier performance notes continue separately.</p><p>Edit… supports names and exact intervals. “Edit pitches on grid…” reopens a saved slot; apply or cancel editor drafts first. Cancel collection preserves that slot. Patterns wider than MIDI’s 128-pitch range remain editable as intervals.</p><h2>Performance and recovery</h2><p>Performance & MIDI controls velocity, Hold Number, capture allowance, channels, outputs, and musical variations. Internal audio and generated MIDI can be enabled independently. Thru is immediate; generated MIDI follows the tick. New instruments route Thru to the selected Thru/generated channel; preserving incoming channels is optional.</p><p>Changing browser focus keeps the performance sounding. If a key release is lost, press and release that control again, or use All Notes Off. Suspended audio offers Resume sound; a processor fault offers Restart sound. Background execution depends on the browser and device.</p>`;
  const examples=`<h2>Learn by playing</h2><p>These modern studies use explicit interval patterns. They are not recovered original preset files. Opening an example uses the normal unsaved-change flow.</p><div class="help-examples">${studies.map((s,i)=>`<article><h3>${s.name}</h3><p>${s.description}</p><small>Chord ${s.chord.join(', ')} · axes ${s.axes.join(' × ')}</small><button data-study="${i}">Open example</button></article>`).join('')}</div>`;
  const about=`<h2>A modern Harmony Grid instrument</h2><p>This implementation follows the original manual and the recorded playing decisions. The example library is newly authored; original binary document import and exact original presets are not available.</p><h2>Sound credits</h2><p>The four sampled sounds are a reduced GeneralUser GS bank by S. Christian Collins, rendered with SpessaSynth Core (Apache-2.0). The unmodified GeneralUser license, source reference, build script, and hashes are included in assets/soundfonts.</p><p>Organ and Pluck are immediate synthesized fallback sounds. The sampled bank prepares before playing starts; no bank parsing occurs during performance. New attacks use the selected sound; existing release tails continue. Synthesis has a fixed 256-voice budget per engine.</p><p>Audio is clocked in an AudioWorklet. Canvas rendering uses a worker where supported, with a main-thread fallback. Export trace includes recent bounded diagnostics; it is not a physical latency measurement.</p>`;
  this.dialog.innerHTML=`<header><div class="eyebrow">Harmony grid</div><h1 id="help-title">Playing reference & examples</h1><nav>${[['guide','Playing guide'],['examples','Examples'],['about','About & sound credits']].map(([id,label])=>`<button data-help="${id}" aria-pressed="${tab===id}">${label}</button>`).join('')}</nav></header><section>${tab==='examples'?examples:tab==='about'?about:guide}</section><footer><button id="help-close">Close</button></footer>`;
  this.dialog.querySelector<HTMLButtonElement>('#help-close')!.onclick=()=>this.dialog.close();
  this.dialog.querySelectorAll<HTMLButtonElement>('[data-help]').forEach(b=>b.onclick=()=>this.render(b.dataset.help!));
  this.dialog.querySelectorAll<HTMLButtonElement>('[data-study]').forEach(b=>b.onclick=()=>{const doc=studyDocument(Number(b.dataset.study));this.dialog.close();this.openExample(doc);});
 }
}
